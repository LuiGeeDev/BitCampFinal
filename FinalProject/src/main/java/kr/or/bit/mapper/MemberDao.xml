<?xml version="1.0" encoding="UTF-8"?>
<!-- 
  void insertMember(Member member);

  void updateMember(Member member);

  void deleteMember(String username);

  List<Member> selectAllMember();
  
  Member selectMemberById(String id);
 -->
<mapper namespace="kr.or.bit.dao.MemberDao">
	<insert id="insertMember" parameterType="kr.or.bit.model.Member">
		insert into member (username, password, name, email, enabled, profile_photo,course_id)
		values(#{username}, #{password}, #{name}, #{email}, #{enabled}, #{profile_photo}, #{course_id})
	</insert>
</mapper>