<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.bit.dao.ArticleOptionDao">
  <select id="selectOption" resultType="kr.or.bit.model.ArticleOption">
    SELECT *
    FROM ${table_name}
    WHERE article_id = #{article_id}
  </select>
</mapper>