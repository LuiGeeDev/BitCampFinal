<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.bit.dao.CategoryDao">
  <select id="selectCategoryByCourseid" 
    resultType="kr.or.bit.model.Category">
    select c.category , c.id 
    from board as b join category as c
    on b.category=c.id
    where c.course_id=#{courseid} group by c.category
    order by b.id asc
  </select>
</mapper>  







  