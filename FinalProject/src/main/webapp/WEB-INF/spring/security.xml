<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
  xmlns="http://www.springframework.org/schema/security"
  xmlns:beans="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.springframework.org/schema/beans
		https://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/security
		https://www.springframework.org/schema/security/spring-security.xsd">

  <beans:bean name="bcryptEncoder"
    class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>
  
  <http>
    <!-- intercept-url은 위에서부터 검색하기 때문에 특정 페이지에 권한을 걸고 싶으면 가장 아래 /** 보다 위에 작성할 것 -->
    <intercept-url pattern="/login*" access="hasRole('ANONYMOUS')"/>
    <intercept-url pattern="/menu" access="hasRole('ADMIN')"/>
    <intercept-url pattern="/**" access="hasRole('USER')"/>
    <form-login 
      login-page="/login"
      login-processing-url="/login"
      default-target-url="/"
      authentication-failure-url="/login-error"/>
    <logout 
      logout-url="/logout"
      logout-success-url="/login"
      delete-cookies="remember_cookie" 
      invalidate-session="true"/>
    <remember-me 
      key="autoLoginKey" 
      token-validity-seconds="604800"
      remember-me-parameter="remember-me"
      remember-me-cookie="remember_cookie"/>
    <csrf disabled="true" />
  </http>
  
  <authentication-manager>
    <authentication-provider>
      <user-service>
        <user name="bituser" password="1004" authorities="ROLE_USER, ROLE_ADMIN"/>
        <user name="raynor" password="1004" authorities="ROLE_USER, ROLE_ADMIN"/>
        <user name="kerrigan" password="1004" authorities="ROLE_USER" />
      </user-service>
    </authentication-provider>
  </authentication-manager>
</beans:beans>